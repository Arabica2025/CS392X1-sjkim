# computer memory
- flat
- cache: 

- traversing list > traversing array (more expensive)

1. Tree-based (e.g. List; have to fetch the memory address to traverse)
2. Array-based (flat; more efficient than Tree-based)